dist: trusty
addons:
  apt:
    packages:
      - cmake
      - iverilog
language: c
install:
  - mkdir build
  - (cd build && cmake ..)
  - make -C build -j2 VERBOSE=1
script: (cd build && ctest --output-on-failure)
